import { resetGlobal } from '@reducers/global';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: 4,
    overflow: 'hidden',
    backgroundColor: "rgba(242, 243, 244, 0.5)",
  },
  indicator: {
    backgroundColor: '#fff',
    height: 4
  }
});
